## 1. 基础与 Schema

- [x] 1.1 定义管理平面领域 schema，覆盖 agent profile 草稿与不可变发布版本。
- [x] 1.2 实现 profile 编译器契约，将分层配置解析为确定性的有效 profile 快照。
- [x] 1.3 实现有效 profile 的版本差异生成能力，用于版本间比较。
- [x] 1.4 定义能力标识与中文展示名映射 schema，保证英文路径稳定、中文展示可配置。

## 2. Agent 内核文件（agent.md、profile.md、soul.md）

- [x] 2.1 定义内核分层模型（组织/项目/agent/环境/会话）并明确优先级顺序。
- [x] 2.2 实现内核组合服务，输出解析后的运行时核心内容与来源归属元数据。
- [x] 2.3 增加 agent.md、profile.md、soul.md 的 API 与 UI 编辑器，支持草稿版本管理和编译预览。

## 3. 内置 Harness 工具治理

- [x] 3.1 定义内置工具策略模型，覆盖 ls、read_file、write_file、edit_file、delete、glob、grep、execute、task。
- [x] 3.2 实现后端能力探测与有效工具面计算（策略意图 + 运行时能力）。
- [x] 3.3 在发布前增加不受支持工具组合的校验门禁。

## 4. 文件系统权限治理

- [x] 4.1 定义有序权限规则 schema，包含操作类型、路径模式与模式（allow/deny）。
- [x] 4.2 实现与 deepagents 权限语义一致的 first-match-wins 评估器。
- [x] 4.3 实现权限模拟接口/UI，展示命中规则与最终决策。

## 5. Human-in-the-Loop 策略与审计

- [x] 5.1 定义按工具/动作和环境维度的中断策略 schema。
- [x] 5.2 将运行时中断策略绑定集成到已发布的有效 profile 中。
- [x] 5.3 实现不可变审批审计事件采集（操作者、时间戳、决策、最终入参）。

## 6. 发布回滚与运行时可追踪

- [x] 6.1 实现发布流程，生成可寻址且不可变的 profile 版本工件。
- [x] 6.2 实现回滚流程，将运行时重新指向历史已发布版本。
- [x] 6.3 确保每条运行轨迹都保存 profile 版本 id 与有效策略/工具摘要。

## 7. API 与 UI 能力面

- [x] 7.1 实现管理平面 API，覆盖草稿、编译预览、发布、回滚与版本历史。
- [x] 7.2 实现管理端 UI 流程，覆盖 profile 编辑、编译预览与环境感知校验错误展示。
- [x] 7.3 实现审计与运行检查视图，覆盖审批记录、工具面与权限结果。
- [x] 7.4 在管理端列表与详情页并列展示“中文能力名 + 英文能力路径”，并支持复制英文路径。

## 8. 菜单驱动信息架构

- [x] 8.1 实现聊天/收件箱、控制、工作区、设置四大导航域。
- [x] 8.2 实现工作区模块：文件、技能、工具、MCP、ACP、运行配置、智能体统计。
- [x] 8.3 实现设置模块：智能体管理、模型管理、技能池、环境变量、安全、Token 消耗、备份、语音转写、调试。

## 9. 验证与发布推广

- [x] 9.1 增加场景驱动测试，覆盖 specs/agent-harness-control-plane/spec.md 的全部 requirement。
- [x] 9.2 增加集成测试，覆盖能力漂移（工具不支持）、中断流程与回滚正确性。
- [x] 9.3 执行分阶段发布，配套迁移检查清单与历史临时配置废弃计划。
